import { SelectedTagFilterPipe } from './selected-tag-filter.pipe';

describe('SelectedTagFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedTagFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
