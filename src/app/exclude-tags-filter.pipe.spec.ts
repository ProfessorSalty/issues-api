import { ExcludeTagsFilterPipe } from './exclude-tags-filter.pipe';

describe('ExcludeTagsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeTagsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
