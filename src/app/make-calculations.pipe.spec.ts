import { MakeCalculationsPipe } from './make-calculations.pipe';

describe('MakeCalculationsPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeCalculationsPipe();
    expect(pipe).toBeTruthy();
  });
});
